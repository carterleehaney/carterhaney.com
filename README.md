### Welcome!

This is a repository for my website. I'm rewriting what previously was just raw HTML and CSS, and making my first full-stack 'application', using Node.js and React. It will eventually contain my writeups, blogs, and various things I want to host.

It can be started by being in the React root directory, and using the command `npm start`

The backend can be started by using the command `node server.js`